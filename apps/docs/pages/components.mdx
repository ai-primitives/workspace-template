# Components

This page demonstrates the usage of custom components from our monorepo.

## Button Component

import { Button } from '@repo/example-ui'

<Button>Click me!</Button>

The button above is imported from our `@repo/example-ui` package, demonstrating how components can be shared and reused across the monorepo.
